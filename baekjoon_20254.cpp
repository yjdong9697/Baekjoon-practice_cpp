#include <bits/stdc++.h>
#define fastio ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0)

using namespace std;

int main(void){
    fastio;
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << a * 56 + 24 * b + 14 * c + 6 * d << "\n";
    return 0;
}