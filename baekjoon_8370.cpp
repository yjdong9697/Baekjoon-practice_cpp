#include <bits/stdc++.h>
#define fastio ios_base::sync_with_stdio(0), cin.tie(0). cout.tie(0)

using namespace std;

int main(void){
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << a * b + c * d << "\n";
    return 0;
}